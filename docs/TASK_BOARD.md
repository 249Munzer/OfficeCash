# لوحة المهام - مشروع OfficeCash

## 📋 ملخص
**المشروع:** OfficeCash - نظام إدارة المقبوضات والمصروفات  
**الإصدار:** 2.0.0 (Electron Desktop)  
**التركيز:** تحسينات محلية فقط (بدون سحابة)  
**آخر تحديث:** 10 أغسطس 2026

---

## 🎯 المرحلة الحالية: **P3 مكتملة — جاهز للإطلاق** ✅

### ✅ ما تم إنجازه بالكامل (P0 → P3)

| المرحلة | الوصف | الحالة |
|----------|--------|-------|
| **P0** | إصلاحات حرجة: ثغرة تبديل الموظف، نصوص مشوهة، كود ميت، أول commit | ✅ مكتملة |
| **P1** | استكمال الوظائف: ربط validation، قفل الأيام، تبويب شهري، توحيد عملة، تعديل المعاملات، رسائل ثنائية اللغة، إخفاء رمز المزامنة | ✅ مكتملة |
| **P2.1** | حزمة اختبارات Vitest + RTL | ✅ مكتملة |
| **P2.2** | Code splitting + manualChunks: الكود الرئيسي 164KB (gzip 47KB)، لا تحذيرات chunk | ✅ مكتملة |
| **P2.3** | عناصر واجهة: زر تفريغ الحقول، closedBy fallback، ترقيم صفحات، تحقق username مكرر، فئات خدمات قابلة للتخصيص/مترجمة | ✅ مكتملة |
| **P2.4** | التوثيق: README شامل + JSDoc + لوحة المهام | ✅ مكتملة |
| **P3** | النشر: أيقونات + إصدار 2.0.0 + معالج NSIS (ترخيص/حماية بيانات) + **فحص يدوي ناجح** | ✅ مكتملة |

### ✅ الحزم المضافة بعد P2.3 (مبنيّة ومُختبَرة — 203 اختبارات)

| الحزمة | الوصف | الحالة |
|--------|--------|-------|
| **الأمان** | استرداد كلمة مرور المدير بأسئلة أمان (حتى 5) + إعادة تصميم إنشاء المكتب (بلا موظفين، موافقة قانونية إلزامية) | ✅ مكتملة |
| **التنقل** | زر تسجيل خروج في الإعدادات + الدخول يبدأ بلوحة التحكم + توحيد نوافذ التبديل (نافذة واحدة) | ✅ مكتملة |
| **الاستحقاقات** | عقد (نسبة 25/40/50/مخصص) + يوم عمل موثّق + تصفية يومية تلقائية + تأكيد مزدوج + شاشة تصفيات | ✅ مكتملة |
| **المثبّت (NSIS)** | معالج خطوات + شروط إلزامية (license AR/EN) + ترخيص حسب لغة النظام + صفحة «الحفاظ على البيانات» في الإلغاء | ✅ مكتملة (بانتظار الفحص اليدوي) |

---

## 📋 مهام قادمة قبل الإطلاق (P3)

### 🟢 P3 — نشر رسمي (مكتمل)
- [x] أيقونة تطبيق مخصصة (`icon.ico`/`icon.png`/`pwa-512.png`)
- [x] `version` = `2.0.0`، `npm run electron:build` ينجح ويُوقَّع
- [x] ملفات الترخيص مربوطة (`installer/license_ar.txt`/`license_en.txt`)
- [x] **فحص يدوي على ويندوز ناجح:** تثبيت نظيف (الخطوات الست + رجوع)، الإلغاء الافتراضي يُبقي `office_cash.db`، الإلغاء الكامل يحذفه، إعادة التثبيت بعد الإلغاء، وترقية نسخة أحدث فوق سابقة

### 🔵 مستقبلاً (اختياري)
- المرحلتان 3–4 من نظام الاستحقاقات (محرر عقود كامل + مركز إشعارات + Voucher)
- تشفير قناة المزامنة، فواتير PDF، تقارير Excel/رسوم بيانية، نسخ احتياطي تلقائي، Auto-updater

| **اختبارات المثبّت اليدوية** | تثبيت نظيف + ترخيص + تغيير مسار + إلغاء افتراضي/كامل + إعادة تثبيت + ترقية ✅ | ✅ مكتملة |

---

## 📊 إحصائيات حالية

| المقياس | القيمة |
|----------|-------|
| **الاختبارات** | 203 اختبار / 18 ملف ✅ |
| **Lint** | 0 أخطاء ✅ |
| **Build** | ناجح، بلا تحذير chunk ✅ |
| **حجم JS الرئيسي** | 164KB (gzip 47KB) ✅ |
| **TypeScript** | 0 أخطاء ✅ |
| **مؤشر الإنجاز** | ~100% — جاهز للإطلاق 🚀 |

---

## 🏗️ البنية التقنية

```
src/
├── App.tsx                    # نقطة الدخول، Lazy-loaded screens + Suspense
├── hooks/                     # 9 Custom Hooks (useAppState, useAuth, useP2PSync, etc.)
├── components/                # شاشات + نوافذ (Dashboard, SettlementsScreen, auth/, ui/, etc.)
├── lib/
│   ├── auth/                  # credentials, registration, securityQuestions, session
│   ├── i18n.ts                # i18n كامل (AR/EN، ~1500 سطر)
│   ├── validation.ts          # التحقق الموحد
│   ├── formatters.ts          # تنسيق التواريخ/العملات/CSV
│   ├── settlement.ts          # محرك الاستحقاقات (نسبة/راتب + تصفية)
│   ├── attendance.ts          # يوم العمل الموثّق (دخول/استراحة/خروج)
│   ├── crypto.ts              # تشفير PINs (Web Crypto API)
│   ├── syncCode.ts            # رموز المزامنة P2P
│   └── electron-storage.ts    # تخزين مشفر (better-sqlite3)
└── types.ts                   # واجهات TypeScript
```

---

## ✅ الإنجازات الأخيرة (10 أغسطس 2026)

### أمان وتسجيل وخروج (4 مهام معتمدة من `todo.md`):
- **استرداد كلمة المرور**: `ForgotPasswordModal` + `securityQuestions.ts` (10 أسئلة، تطبيع، hash/verify، حد 5 محاولات) + رابط «نسيت كلمة المرور» في `AdminLoginForm` + `resetAdminPin` في `AuthProvider`.
- **إعادة تصميم إنشاء المكتب**: `OfficeRegistrationForm` بلا موظفين — معلومات المكتب + PIN + أسئلة أمان (2–5) + موافقة قانونية إلزامية (`LegalModal`).
- **زر تسجيل الخروج**: بطاقة «تسجيل الخروج» أسفل منطقة الخطر في `SettingsManager` + العودة دائماً للوحة التحكم بعد الدخول.
- **توحيد نوافذ التبديل**: إغلاق Switcher Modal قبل فتح `AuthModal` + `initialTab` في `AuthModal`.

### نظام الاستحقاقات (المرحلتان 1–2):
- `settlement.ts` (نسب 25/40/50/مخصص + `computeCommission` + `buildDailySettlement` + `employeeWallet`)، `attendance.ts` (دخول/استراحة/خروج)، `SettlementsScreen.tsx` (تأكيد صرف/استلام مزدوج)، عقد الموظف في `EmployeesManager`، بث P2P للحضور/التصفيات.

### المثبّت الاحترافي (NSIS Wizard):
- `oneClick: false` + `allowToChangeInstallationDirectory` + `perMachine: false` + `installerLanguages` (en_US/ar).
- `installer/installer.nsh`: صفحة ترحيب «نبذة عن التطبيق» + صفحة إلغاء «الحفاظ على البيانات» + حماية `office_cash.db`.
- إصلاح أخطاء NSIS: `PageEx un.custom` بدل `Page custom` (6010)، `MUI_FINISHPAGE_TEXT` عبر define، تغليف `UnKeepDataCheckbox` بـ `BUILD_UNINSTALLER` (6001).
- ربط الترخيص: `"license": "installer/license_ar.txt"` (AR/EN حسب لغة النظام).

---

## 🔗 مراجع

- `docs/COMPLETION_WORK_PLAN.md` — خطة العمل التفصيلية مع معايير القبول
- `docs/INSTALLER_UNINSTALLER_WORK_PLAN.md` — خطة معالج التثبيت/الإلغاء الاحترافي
- `docs/EMPLOYEE_SETTLEMENT_WORK_PLAN.md` — خطة نظام الاستحقاقات والتصفية
- `docs/PROFESSIONAL_REPORT_FINAL.md` — تقرير حالة المشروع الكامل
- `README.md` — دليل المستخدم والمطور الشامل

---

**الحالة:** 🟢 **جاهز للإطلاق** — P0 → P3 مكتملة، الفحص اليدوي ناجح (10 أغسطس 2026)